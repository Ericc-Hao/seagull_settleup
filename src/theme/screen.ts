import { colors } from './colors';

/** Shared screen root style — use for page/viewport backgrounds, not cards. */
export const screenBackgroundStyle = {
  flex: 1,
  backgroundColor: colors.background,
} as const;
